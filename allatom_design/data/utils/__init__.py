"""Shared, pure data-layer helpers.

These modules hold cfg-free primitives reused across the SD dataset, the
preprocessing pipelines and legacy code:

- :mod:`pn_unit` -- pn_unit_iid / contact-list / CCD parsing and ligand keys.
- :mod:`nucleic_acid_groups` -- nucleic-acid group computation.
"""
